<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Tầng trong cùng: Entity + Enum + Constant.
    KHÔNG tham chiếu project nào, KHÔNG tham chiếu package nào.
    Nhờ vậy quy tắc nghiệp vụ (thang điểm, xếp loại) không dính EF hay WPF.
  -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

</Project>
